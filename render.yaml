services:
  - type: web
    name: edalab-backend
    runtime: node
    rootDir: backend
    plan: free
    healthCheckPath: /api/health
    buildCommand: npm install && npm run build
    preDeployCommand: npx prisma db push
    startCommand: npm run start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CORS_ORIGIN
        value: "*"
      - key: PUBLIC_BASE_URL
        value: "https://edalab.onrender.com"
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: SUPABASE_STORAGE_BUCKET_AVATARS
        value: avatars
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: JWT_SECRET
        sync: false
